
{
  "entities": {
    "User": {
      "title": "User",
      "description": "Base user profile.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "userType": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "StudentInterest": {
      "title": "StudentInterest",
      "description": "Student's tuition enquiry.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "studentName": { "type": "string" },
        "subject": { "type": "string" },
        "school": { "type": "string" },
        "gradeOrClass": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "TeacherInterest": {
      "title": "TeacherInterest",
      "description": "Teacher's specialty profile.",
      "type": "object",
      "properties": {
        "teacherId": { "type": "string" },
        "teacherName": { "type": "string" },
        "subjects": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "MatchProposal": {
      "title": "MatchProposal",
      "description": "Matching record between student enquiry and teacher.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "enquiryId": { "type": "string" },
        "teacherId": { "type": "string" },
        "status": { "type": "string" }
      }
    },
    "Notification": {
      "title": "Notification",
      "description": "Platform event notification.",
      "type": "object",
      "properties": {
        "type": { "type": "string" },
        "subject": { "type": "string" },
        "body": { "type": "string" },
        "read": { "type": "boolean" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/User" },
      "description": "User profiles."
    },
    "/studentInterests/{interestId}": {
      "schema": { "$ref": "#/backend/entities/StudentInterest" },
      "description": "Student tuition enquiries."
    },
    "/teacherInterests/{interestId}": {
      "schema": { "$ref": "#/backend/entities/TeacherInterest" },
      "description": "Teacher specialty profiles."
    },
    "/matchProposals/{matchId}": {
      "schema": { "$ref": "#/backend/entities/MatchProposal" },
      "description": "Match proposals."
    },
    "/notifications/{notificationId}": {
      "schema": { "$ref": "#/backend/entities/Notification" },
      "description": "System and user notifications."
    },
    "/systemLogs/{logId}": {
      "schema": "object",
      "description": "Admin activity logs."
    }
  }
}
