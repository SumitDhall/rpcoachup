
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Broadest possible access for development and troubleshooting.
    // This removes all authentication and role-based restrictions.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
